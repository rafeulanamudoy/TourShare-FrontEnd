// "use server";

// import { cookies } from "next/headers";

// export const getCookies = async (value: string) => {
//   const cookieStore = cookies();
//   const cookie = cookieStore.get(value)?.value;
//   return cookie ?? null;
// };
